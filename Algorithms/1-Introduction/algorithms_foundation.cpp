// What are algorithms?

int main() {


    // sorting shapes
    for (each shape) {
        if (shape is ellipse) {
            ellipses.add(shape)
        }
        if (shape is rectangle) {
            rectangles.add(shape)
        }
        if (shape is triangle) {
            triangles.add(shape)
        }
    }   
}

